"""App configuration for mcp_tools_core."""

from django.apps import AppConfig


class McpToolsCoreConfig(AppConfig):
    """Configuration for the MCP Tools Core app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "mcp_tools_core"
    verbose_name = "MCP Tools Core"

